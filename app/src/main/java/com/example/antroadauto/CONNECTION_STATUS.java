package com.example.antroadauto;

public enum CONNECTION_STATUS {
    DISCONNECTED,
    CONNECTED,
    CONNECTING,
    HOSTING,
    FAILED,
    AWAITING_CONNECTION,
    CLIENT_CONNECTED,
    STOPPING
}
